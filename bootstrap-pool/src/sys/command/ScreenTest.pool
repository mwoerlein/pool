/*[meta]
mimetype = text/x-pool
author = Marc Woerlein <marc.woerlein@gmx.de>
version = 0.1.0
*/
namespace sys::command;


class ScreenTest extends linux::Command {

    [] configure() {
        this.setName("screentest")
            .setVersion("0.1.0")
            .setUsage(<"
Test for some screen features.

    Usage:
      screentest [options]

    Options:
      -h --help                     Show this screen.
      --version                     Show version.
">);
        this.registerOptionAlias('h', "help")
        ;
    }

    [int] run() {
        sys::stream::OStream out = this.rt().out();
        if (this.hasOption("help")) {
            out.printCString(usage).printNewline();
            return 0;
        }
        
        linux::LinuxScreen ls = this.initScreen();
        sys::tui::WindowManager wm = this.createOwn(sys::tui::WindowManager:CLASSNAME);
/*        
        sys::stream::FOStream fout = ls.createFOStream(13, 4);
        fout.printChar('$').printInt(ls.getMaxX()).printChar('x').printInt(ls.getMaxY()).printNewline();
        fout.printChar('$').printInt(ls.getMaxX()).printChar('x').printInt(ls.getMaxY()).printNewline();
*/
        
        int x1 = (ls.getMaxX() / 2) - 1;
        int x2 = ls.getMaxX() - x1 - 3;
        int y2 = ls.getMaxY() / 5;
        int y1 = ls.getMaxY() - y2 - 3;
        wm._init(ls.screen()).draw3TiledFrame(x1, x2, y1, y2);
        
        ls.drawFCharAt('#',1,2,98,1);
        ls.drawFCharAt('#',1,2,99,2);
        ls.drawFCharAt('#',1,2,100,3);
        ls.drawFCharAt('#',1,2,101,4);
        ls.drawFCharAt('#',1,2,102,5);
        ls.drawFCharAt('#',1,2,103,6);
        ls.drawFCharAt('#',1,2,104,7);
        ls.drawFCharAt('#',1,2,105,8);
        ls.drawFCharAt('#',1,2,106,9);
        ls.drawFCharAt('#',1,2,107,10);
        ls.drawFCharAt('#',1,2,108,11);
        ls.drawFCharAt('#',1,2,109,12);
        ls.drawFCharAt('#',1,2,110,13);
//        ls.drawFCharAt('#',1,2,0,0);
//        ls.drawFCharAt('#',1,2,ls.getMaxX()-1,ls.getMaxY()-1);

        // wait bevor termination which clears screen again
        this.rt().sleep(10, 0);
    }
}
